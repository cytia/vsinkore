# vsinkore 阶段工作表

VSCode 插件：把 `@inkore/editor-core` 作为 host，注册为 `.md` 的 Custom Text Editor。纯免费、只做编辑。

**定位**：面向开发者的文档查阅 / 编辑场景——在 VSCode 里所见即所得地看 / 改项目文档、快速定位信息。**视觉完全对齐 VSCode**（用原生主题色变量与字体、跟随明暗），不移植砚客主题。`[D0-5]`

决策 / 契约 / 踩坑见 [阶段决策备忘.md](阶段决策备忘.md)（`[Dx-y]` 锚点）。

---

## 首版功能范围（已敲定）

| 项 | 决定 |
|---|---|
| 行内格式 | 加粗 / 斜体 / 行内代码 / 下划线 / 删除线 / 文字颜色 / 链接 ✅ |
| 块 | 标题 H1–H6 / 正文 / 引用 / 代码块 / 无序·有序·任务列表 / 分割线 ✅ |
| 代码块高亮 | **Shiki（VSCode 同款）+ 插件侧覆盖 NodeView**，跟随 VSCode 主题；块内不另设行号 `[D0-8]` |
| 重节点 | 表格 / 行内·块级公式 / 脚注 / 目录 **全启用** ✅ |
| Bubble Toolbar | ✅（UI 用 VSCode 主题变量自写） |
| 右键菜单 | ✅（格式 + 插入 + 粘贴为纯文本） |
| 输入规则速打 | ✅（内核现成） |
| 查找高亮 | ✅（内核 searchHighlight） |
| 图片 | **只读显示**已有图片（`toRenderUrl`→`asWebviewUri`）；**不支持插入 / 粘贴 / 拖入**（`saveImage` no-op）`[D0-6]` |
| 行号 | **左侧行号槽 + 源码行号口径**（对齐 .md 真实行号，服务于定位）`[D0-7]` |
| 视觉 / 主题 | **完全对齐 VSCode**（原生 CSS 变量 + 跟随明暗，不移植砚客主题）`[D0-5]` |

---

## 阶段零：方案敲定 ✅

- [X] 通读内核 `Inkore-core`（导出面 / host 注入 / setup / example 参照）
- [X] 形态选定：Custom Text Editor（非独立面板）`[D0-1]`
- [X] 付费边界选定：纯免费、只做编辑，不碰激活 / PRO / 导出 `[D0-2]`
- [X] 仓库落点选定：独立仓 `D:\My-Projects\vsinkore`，内核 `file:../Inkore-core` 引入 `[D0-3]`
- [X] 构建选定：esbuild 双 bundle `[D0-4]`
- [X] 项目规范落地（CLAUDE.md + 工作规范 + 本表 + 决策备忘）

---

## 阶段一：仓库骨架（F5 起空 webview）

- [X] `package.json`：`contributes.customEditors`（viewType + `.md` selector）、activation、scripts
- [X] esbuild 双 bundle 构建脚本（extension Node/CJS + webview browser）
- [X] `.vscode/launch.json`：F5 起扩展开发宿主
- [X] `CustomTextEditorProvider` 最小实现：打开 `.md` 渲染一个空 webview（CSP + nonce + asWebviewUri 骨架）
- [X] `.gitignore`（node_modules / dist / out）
- [X] 验证：F5 → 打开任意 `.md` → Reopen With Inkore Editor → 看到 webview（"alive" 占位）

---

## 阶段二：内核跑起来（PoC，只读不回写）

- [X] webview 引入内核：`createEditorState`（`saveImage` 传 no-op）+ 全套 NodeView wiring（参照 `example/main.ts`）
- [X] 排版 CSS：取桌面版 Editor.module.css 的**排版结构**（行距 / 间距 / 块样式），颜色 / 字体一律改用 VSCode 原生变量，对齐本仓 `原型/editor-mockup.html` `[D0-5]` `[D2-1]`
- [X] extension 读 `document.getText()` → push 给 webview → `parseMarkdown` 渲染
- [X] katex / prosemirror CSS 打包（不走 CDN）
- [X] 代码块语法高亮：Shiki（JS 引擎，无 WASM）+ ProseMirror inline decoration 给 `<code>` 上色（不动 contentDOM，保留内核 header）；按需懒加载语言；nonce 授权 `<style>` 注入 token 色表（严守 CSP）；主题跟随 VSCode 明暗 `[D0-8]`。F5 验证：dark-plus 配色生效、零 CSP/chunk 报错
- [X] 修复：语言下拉被 `.pm-code-block { overflow:hidden }` 裁切。去掉外层 overflow，圆角改由 header（上两角）/ pre（下两角）各自承担。F5 验证下拉完整展开
- [X] 验证：打开真实 `.md` → 看到所见即所得渲染，配色对齐原型，能编辑（先不回写磁盘）

**渲染异常已复现 + 分类**（headless 跑 `parseMarkdown` 定位）：
- _成因一（已修 · F5 验证）_：math / 脚注 / 代码块下拉等 nodeView **缺 host CSS**，渲染裸 DOM。已在 `media/editor.css` 补齐，贴合代码块卡片风格、全 `--vscode-*` 变量。F5 渲染正常。
- _成因二（待办 · 内核侧）_：标准 Markdown 脚注定义 `[^1]: 文本` 内核不解析（只认自身 `<fn-def>` HTML 形式），且往返一趟会丢定义、乱编号。内核源码已内联（见下），可作为独立任务在 `src/editor-core/` 内修——需连带解析 / 序列化 / 同步插件三处一起改，并定序列化口径。

**结构变更：内核源码内联** `[D-inline]`：内核由 `file:../Inkore-core` 改为源码拷入 `src/editor-core/`，依赖收编进本仓，删 esbuild ProseMirror alias（`[D2-2]` 修法作废，单 node_modules 自然去重）。`npm ls` 单副本、构建 + typecheck 通过。

---

## 阶段三：双向桥（编辑回写 + 外部同步）

- [X] 消息协议文件（extension↔webview 共用 TS 类型）：`WebviewMessage` 加 `edit`、`ExtensionMessage` 加 `update`
- [X] webview 改动 → `serializeMarkdown` → postMessage；序列化延迟进防抖 thunk，只对存活的那次编辑序列化（不按键逐次走全树）`[D3-1]`
- [X] extension 收到 → `writeBack` 用 `WorkspaceEdit` 整文替换 document（文本相同则 no-op，不脏标记；VSCode 管脏标记 / 保存 / undo）
- [X] 防回环：`lastWrittenText` 文本比对识别"插件自己刚写的"变更，不回推 webview；apply 前记录以避开同步 change 事件竞态 `[D3-1]`
- [X] 外部改动（其他编辑器 / git 切分支）→ 推 `update` 给 webview，`applyExternal` 整文替换 doc 重渲染（保留 view/插件/滚动，不 remount）`[D3-1]`
- [X] 验证：编辑→Ctrl+S 落盘正确；外部改文件→webview 同步；连续打字不跳光标（F5 通过）

---

## 阶段四：图片只读显示 + 主题对齐

- [X] `toRenderUrl` 桥：文档内相对路径 → extension `asWebviewUri` 解析的 base → webview 侧 `new URL` 拼接渲染。图片根 = **vault 根（workspace folder）**，非文档目录（修正口径见 `[D4-1]`）；localResourceRoots 加 vault 根 `[D0-6]`
- [X] `saveImage` 给 no-op：粘贴 / 拖入图片静默忽略，不落盘 `[D0-6]`
- [X] 主题对齐：图片样式接 `--vscode-*` 变量（阶段二排版 CSS 已明暗跟随，本阶段只补图片规则）`[D0-5]`
- [X] 验证：子文件夹内笔记的 `.images/` vault 根图片正确显示；明暗主题跟随；粘贴图片不报错（F5 通过）

---

## 阶段五：行号槽 + 交互打磨

- [X] 行号槽（源码行号口径）：插件侧用内核导出的 mdIt 平行解析取 `token.map`，与顶层节点 1:1 zip 出每块源码起始行；绝对定位兄弟层按 `coordsAtPos` 对齐块顶渲染（无竖线、右对齐、逐块标首行），rAF 节流。F5 通过：行号与 .md 真实物理行号一致（跳号=块间空行，符合 `[D0-7]` 口径）`[D5-1]`
- [X] Bubble Toolbar UI：内核 `bubbleToolbarPlugin` 给选区几何 + `calcBubblePosition` 定位，host 自写浮层接 command（内核零改动）；UI 严格复刻 `原型/editor-mockup.html` 的 `.bubble`（B/I/U/S/`</>`/分隔/单色 swatch/🔗）；文字色只一个（swatch 显 `--vscode-charts-purple`，入档固定 hex `#c586c0`）；VSCode 全局 `fadeIn` 浮出动画；按钮 VSCode 同款自绘 tooltip（通用 `tooltip.ts`，查找框共用）。F5 通过 `[D5-4]`
- [X] 编辑器右键菜单 UI：host 自建单层菜单（格式 / 插入 / 编辑 三分组 + 表格行列二级浮层），UI 对齐 VSCode 菜单、条目对齐 Inkore 原版，接内核 command 集（toggleBlockquote/CodeBlock/三种 list、insert hr/footnote/math/toc、insertTable、pasteAsPlainText）；链接复用 bubble 的链接输入浮层（`mountLinkInput` 上提到 editorHost 共享）；图片项按 `[D0-6]` 只读范围去掉。内核零改动 `[D5-5]`
  - [X] 表格内右键菜单：`contextMenu.ts` 按内核 `isInTable` 分发——表格外弹格式菜单、表格内弹独立的行/列/单元格编辑菜单（`contextMenuTableEdit.ts`，对齐 Inkore 原版），接 `addRow/Column`、`deleteRow/Column`、`mergeCells/splitCell/deleteTable`。两套不同时出现 `[D5-5]`
  - [X] 表格内精简 Bubble：`mountBubbleToolbar` 加 `compact` 变体（仅 B/I/U/S/`</>`/标色/链接），`editorHost` 按 `isInTable` 在完整版与精简版间分发；标色/链接与正文版一致。内核零改动 `[D5-6]`
  - [X] 链接打开：Ctrl/Cmd+左键命中 `<a>` → webview `mousedown` 截获 → `openLink` 消息 → extension `vscode.env.openExternal`（仅放行 http(s)）。普通点击仍摆光标 `[D5-5]`
  - [X] 打磨：文字标色由紫改绿（swatch `--vscode-charts-green`、入档 `#4ec9b0`）；任务列表打勾框与文字对齐修复 + 勾选态用 VSCode token 上色；popup 定位/菜单渲染抽公共件（`popupPosition.ts` / `menuRows.ts`）
  - [X] 修复：行号槽空段落错位——空 `<p>` 是 PM 顶层节点但 markdown-it 不出块，按 index zip 漂移、末块丢号；`lineGutter` 遍历跳过空段落使有内容块与块行号重新 1:1（`[D5-1]` 修正）
  - [X] 修复：KaTeX 公式塌（上下标/分式跑位）——CSP `style-src` 缺 `unsafe-inline`，KaTeX 逐元素 inline style 被全拦；改 `style-src` 为 `cspSource 'unsafe-inline'`、去 style nonce（script nonce 不动），取代 `[D0-8]` CSP 口径 `[D5-7]`
  - [X] 修复：公式块输入框右下角原生 resize 白方块（深色下突兀）→ `resize: none`
  - [X] 修复：表格子浮层在窗口右缘溢出窗外——`placeRightOf` 加横向翻转（右侧放不下翻到菜单左侧）+ 纵向 clamp
- [X] 查找高亮：webview 内自建查找框（VSCode 原生 find 够不到 iframe 内部），驱动内核 `searchHighlightPlugin`（内核零改动）；`Ctrl/Cmd+F` 开/`Esc` 关，Aa/.* 选项、`n/m` 计数、↑↓·Enter 循环导航滚到 match。F5 通过 `[D5-2]`
- [X] 空态 / 加载态：**空态**内核 `placeholderPlugin` + `.pm-placeholder` 已覆盖（空文档 ghost text，未重做）；**加载态**新增——provider HTML 预置 `<div class="pm-loading">Loading…</div>`，`init` 到达 webview `mount.textContent=""` 自动抹掉无缝切。同时把 `customEditors.priority` 由 `option` 改 `default`，`.md` 打开直接进 vsinkore（不用手动 Reopen）。F5 通过 `[D5-3]`
- [X] 验证：F5 跑真实文档（含标题/列表/任务列表/代码块/表格/公式/链接/脚注/图片）——行号与 .md 真实行号对齐、核心编辑交互齐全；期间修复行号空段落错位、KaTeX 公式 CSP、公式框 resize、表格子浮层溢出共 4 处，逐一 F5 复验通过

> **技术风险（行号）已关闭** `[D5-1]`：WYSIWYG 块反查源码行经 headless 验证可行——走插件侧平行解析（内核零改动），非改内核加 sourceLine attr。过滤口径 `level===0 && nesting>=0 && map`（含 fence/hr 等 nesting:0 块，否则错位）。`[D0-7]`

---

## 待办 / 待评估（独立时机）

- [~] 打包发布：**本地 `.vsix` 已出**（`vsinkore-0.1.0.vsix`，`@vscode/vsce` 打包）——package.json 删 `private`、版本 0.1.0、补 `repository`/`categories`/`icon`（`media/icon.png` 由桌面版图标 SVG 内嵌 PNG 提取）；`.vscodeignore` 只留 dist + media + README；README 作详情页。**上架 Marketplace 待办**：需注册 publisher（`inkore` 占位）+ Azure DevOps PAT，单独评估
- [ ] 大纲视图（内核已有 `extractOutline`，可接 VSCode 侧栏）
