# vsinkore 阶段工作表

VSCode 插件：把 `@inkore/editor-core` 作为 host，注册为 `.md` 的 Custom Text Editor。纯免费、只做编辑。

决策 / 契约 / 踩坑见 [阶段决策备忘.md](阶段决策备忘.md)（`[Dx-y]` 锚点）。

---

## 阶段零：方案敲定 ✅

- [X] 通读内核 `Inkore-core`（导出面 / host 注入 / setup / example 参照）
- [X] 形态选定：Custom Text Editor（非独立面板）`[D0-1]`
- [X] 付费边界选定：纯免费、只做编辑，不碰激活 / PRO / 导出 `[D0-2]`
- [X] 仓库落点选定：独立仓 `D:\My-Projects\vsinkore`，内核 `file:../Inkore-core` 引入 `[D0-3]`
- [X] 构建选定：esbuild 双 bundle `[D0-4]`
- [X] 项目规范落地（CLAUDE.md + 工作规范 + 本表 + 决策备忘）

---

## 阶段一：仓库骨架（F5 起空 webview）

- [ ] `package.json`：`contributes.customEditors`（viewType + `.md` selector）、activation、scripts
- [ ] esbuild 双 bundle 构建脚本（extension Node/CJS + webview browser）
- [ ] `.vscode/launch.json`：F5 起扩展开发宿主
- [ ] `CustomTextEditorProvider` 最小实现：打开 `.md` 渲染一个空 webview（CSP + nonce + asWebviewUri 骨架）
- [ ] `.gitignore`（node_modules / dist / out）
- [ ] 验证：F5 → 打开任意 `.md` → 看到插件 webview（暂无编辑器）

---

## 阶段二：内核跑起来（PoC，只读不回写）

- [ ] webview 引入内核：`createEditorState` + 全套 NodeView wiring（参照 `example/main.ts`）
- [ ] 移植编辑器 CSS（桌面版 Editor.module.css + tokens 编辑器相关部分）进 `media/`，打进 webview bundle
- [ ] extension 读 `document.getText()` → push 给 webview → `parseMarkdown` 渲染
- [ ] katex / prosemirror CSS 打包（不走 CDN）
- [ ] 验证：打开真实 `.md` → 看到所见即所得渲染，能编辑（先不回写磁盘）

---

## 阶段三：双向桥（编辑回写 + 外部同步）

- [ ] 消息协议文件（extension↔webview 共用 TS 类型）
- [ ] webview 改动 → `serializeMarkdown` → postMessage
- [ ] extension 收到 → `WorkspaceEdit` 整文替换 document（VSCode 管脏标记 / 保存 / undo）
- [ ] 防回环：识别"插件自己刚写的"变更，不回推 webview `[D3-?]`
- [ ] 外部改动（其他编辑器 / git 切分支）→ 推 doc 给 webview 重渲染
- [ ] 验证：编辑→Ctrl+S 落盘正确；外部改文件→webview 同步

---

## 阶段四：图片 + 主题

- [ ] `saveImage` 桥：webview 粘贴 / 拖入图片 → postMessage → extension 写工作区图片目录 → 返相对路径
- [ ] `toRenderUrl` 桥：相对路径 → extension `asWebviewUri` → 渲染（localResourceRoots 加图片目录）
- [ ] VSCode 主题映射：`ColorTheme` 明 / 暗 → webview `data-theme`
- [ ] 验证：粘贴图片落盘 + 重开正确渲染；切 VSCode 主题编辑器跟随

---

## 阶段五：交互打磨

- [ ] Bubble Toolbar UI（内核给位置计算 + 选区信息，UI 自写）
- [ ] 编辑器右键菜单 UI（接内核 command 集）
- [ ] 查找高亮（接内核 searchHighlight）
- [ ] 空态 / 加载态
- [ ] 验证：核心编辑交互齐全，跑一遍真实写作流程

---

## 待办 / 待评估（独立时机）

- [ ] 内核发 npm 后，`file:` 依赖切正式版本号
- [ ] 插件发布到 VSCode Marketplace（需 publisher 账号，发布前单独评估）
- [ ] 大纲视图（内核已有 `extractOutline`，可接 VSCode 侧栏）
