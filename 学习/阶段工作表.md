# vsinkore 阶段工作表

VSCode 插件：把 `@inkore/editor-core` 作为 host，注册为 `.md` 的 Custom Text Editor。纯免费、只做编辑。

**定位**：面向开发者的文档查阅 / 编辑场景——在 VSCode 里所见即所得地看 / 改项目文档、快速定位信息。**视觉完全对齐 VSCode**（用原生主题色变量与字体、跟随明暗），不移植砚客主题。`[D0-5]`

决策 / 契约 / 踩坑见 [阶段决策备忘.md](阶段决策备忘.md)（`[Dx-y]` 锚点）。

---

## 首版功能范围（已敲定）

| 项 | 决定 |
|---|---|
| 行内格式 | 加粗 / 斜体 / 行内代码 / 下划线 / 删除线 / 文字颜色 / 链接 ✅ |
| 块 | 标题 H1–H6 / 正文 / 引用 / 代码块 / 无序·有序·任务列表 / 分割线 ✅ |
| 代码块高亮 | **Shiki（VSCode 同款）+ 插件侧覆盖 NodeView**，跟随 VSCode 主题；块内不另设行号 `[D0-8]` |
| 重节点 | 表格 / 行内·块级公式 / 脚注 / 目录 **全启用** ✅ |
| Bubble Toolbar | ✅（UI 用 VSCode 主题变量自写） |
| 右键菜单 | ✅（格式 + 插入 + 粘贴为纯文本） |
| 输入规则速打 | ✅（内核现成） |
| 查找高亮 | ✅（内核 searchHighlight） |
| 图片 | **只读显示**已有图片（`toRenderUrl`→`asWebviewUri`）；**不支持插入 / 粘贴 / 拖入**（`saveImage` no-op）`[D0-6]` |
| 行号 | **左侧行号槽 + 源码行号口径**（对齐 .md 真实行号，服务于定位）`[D0-7]` |
| 视觉 / 主题 | **完全对齐 VSCode**（原生 CSS 变量 + 跟随明暗，不移植砚客主题）`[D0-5]` |

---

## 阶段零：方案敲定 ✅

- [X] 通读内核 `Inkore-core`（导出面 / host 注入 / setup / example 参照）
- [X] 形态选定：Custom Text Editor（非独立面板）`[D0-1]`
- [X] 付费边界选定：纯免费、只做编辑，不碰激活 / PRO / 导出 `[D0-2]`
- [X] 仓库落点选定：独立仓 `D:\My-Projects\vsinkore`，内核 `file:../Inkore-core` 引入 `[D0-3]`
- [X] 构建选定：esbuild 双 bundle `[D0-4]`
- [X] 项目规范落地（CLAUDE.md + 工作规范 + 本表 + 决策备忘）

---

## 阶段一：仓库骨架（F5 起空 webview）

- [X] `package.json`：`contributes.customEditors`（viewType + `.md` selector）、activation、scripts
- [X] esbuild 双 bundle 构建脚本（extension Node/CJS + webview browser）
- [X] `.vscode/launch.json`：F5 起扩展开发宿主
- [X] `CustomTextEditorProvider` 最小实现：打开 `.md` 渲染一个空 webview（CSP + nonce + asWebviewUri 骨架）
- [X] `.gitignore`（node_modules / dist / out）
- [X] 验证：F5 → 打开任意 `.md` → Reopen With Inkore Editor → 看到 webview（"alive" 占位）

---

## 阶段二：内核跑起来（PoC，只读不回写）

- [ ] webview 引入内核：`createEditorState`（`saveImage` 传 no-op）+ 全套 NodeView wiring（参照 `example/main.ts`）
- [ ] 排版 CSS：取桌面版 Editor.module.css 的**排版结构**（行距 / 间距 / 块样式），颜色 / 字体一律改用 VSCode 原生变量（`--vscode-editor-*` / `--vscode-foreground` 等），不移植砚客主题 `[D0-5]`
- [ ] extension 读 `document.getText()` → push 给 webview → `parseMarkdown` 渲染
- [ ] katex / prosemirror CSS 打包（不走 CDN）
- [ ] 代码块语法高亮：插件侧自写 `code_block` NodeView（`prosemirror-highlight` + Shiki JS 引擎），覆盖内核 NodeView；按需加载语言；主题跟随 VSCode 明暗 `[D0-8]`
- [ ] 验证：打开真实 `.md` → 看到所见即所得渲染（含表格 / 公式 / 脚注 / 目录 / **高亮代码块**），能编辑（先不回写磁盘）

---

## 阶段三：双向桥（编辑回写 + 外部同步）

- [ ] 消息协议文件（extension↔webview 共用 TS 类型）
- [ ] webview 改动 → `serializeMarkdown` → postMessage
- [ ] extension 收到 → `WorkspaceEdit` 整文替换 document（VSCode 管脏标记 / 保存 / undo）
- [ ] 防回环：识别"插件自己刚写的"变更，不回推 webview `[D3-?]`
- [ ] 外部改动（其他编辑器 / git 切分支）→ 推 doc 给 webview 重渲染
- [ ] 验证：编辑→Ctrl+S 落盘正确；外部改文件→webview 同步

---

## 阶段四：图片只读显示 + 主题对齐

- [ ] `toRenderUrl` 桥：文档内相对路径 → extension `asWebviewUri` → 渲染（localResourceRoots 加文件所在目录）`[D0-6]`
- [ ] `saveImage` 给 no-op：粘贴 / 拖入图片静默忽略或提示"插件暂不支持插图"，不落盘 `[D0-6]`
- [ ] 主题对齐：编辑器配色 / 字体接 VSCode CSS 变量，明暗自动跟随（无需自管 `data-theme`）`[D0-5]`
- [ ] 验证：含截图的文档能正确显示已有图片；切 VSCode 明 / 暗主题编辑器跟随；粘贴图片不报错

---

## 阶段五：行号槽 + 交互打磨

- [ ] 行号槽（源码行号口径）：`parseMarkdown` 时把每个顶层块的源码起始行带进节点 / 映射表，渲染时左侧画行号对齐块起始行 `[D0-7]`
- [ ] Bubble Toolbar UI（内核给位置计算 + 选区信息，UI 自写、用 VSCode 主题变量）
- [ ] 编辑器右键菜单 UI（接内核 command 集：格式 / 插入 / 粘贴为纯文本）
- [ ] 查找高亮（接内核 searchHighlight）
- [ ] 空态 / 加载态
- [ ] 验证：行号与 .md 真实行号对齐；核心编辑交互齐全，跑一遍真实文档查阅 / 编辑流程

> **技术风险（行号）**：WYSIWYG 块反查源码行是内核没有的能力，可能需在内核加"带 sourceLine 的解析"或插件侧自建映射。阶段五先验证可行性再铺开。`[D0-7]`

---

## 待办 / 待评估（独立时机）

- [ ] 内核发 npm 后，`file:` 依赖切正式版本号
- [ ] 插件发布到 VSCode Marketplace（需 publisher 账号，发布前单独评估）
- [ ] 大纲视图（内核已有 `extractOutline`，可接 VSCode 侧栏）
