// Webview entry (browser sandbox). Skeleton stage: prove the nonced script
// runs under CSP. The editor core wiring lands in stage two.
const root = document.getElementById("root");
if (root) {
  root.textContent = "Inkore editor webview is alive.";
}
